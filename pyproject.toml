[project]
name = "rkllama-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ddgs>=9.10.0",
    "gradio>=6.3.0",
    "huggingface-hub>=1.3.2",
    "pip>=25.3",
]
